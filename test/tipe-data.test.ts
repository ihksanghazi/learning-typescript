describe("data type", () => {
	it("should must be declare", function () {
		const name: string = "Nursandy Ihksan";
		const balance: number = 100;
		const isVIP: boolean = true;

		console.info(name);
		console.info(balance);
		console.info(isVIP);
	});
});
